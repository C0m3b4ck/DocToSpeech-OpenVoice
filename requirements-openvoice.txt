# ========================= OPENVOICE (separate venv) =========================
# Why separate: needs git clone + MeloTTS, cleaner to isolate
#
# IMPORTANT: Use Python 3.11 (or 3.10-3.13). Python 3.14 has no torch==2.6.0 wheels.
#
# Setup:
#   python3.11 -m venv .venv-openvoice
#   source .venv-openvoice/bin/activate
#   pip install -r requirements-openvoice.txt
#   git clone https://github.com/myshell-ai/OpenVoice.git
#   cd OpenVoice && pip install -e . && cd ..
#   pip install git+https://github.com/myshell-ai/MeloTTS.git
#   python -c "from huggingface_hub import snapshot_download; snapshot_download(repo_id='myshell-ai/OpenVoiceV2', local_dir='checkpoints_v2')"
#   python -m unidic download
#
# Then run doctospeech from this venv.

epub2txt==0.1.6
docx2txt==0.9
pdftotext==4.0.0
html2text==2025.4.15
pyfiglet==1.0.4
tqdm==4.68.4
rich==15.0.0
ollama==0.6.2

torch==2.6.0
torchaudio==2.6.0

# ========================= GRADIO WEB UI =========================
gradio>=5.0.0
